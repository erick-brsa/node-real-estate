extends ../layout/index

block content
    div(class="py-10 my-10")
        h2(class="text-center text-4xl font-extrabold my-10") Bienes
            span(class="font-normal") Raices
        h2(class="text-center text-2xl font-extrabold")= page

        div(class="bg-white shadow py-8 px-4 rounded-lg mx-auto max-w-md")
            p(class= error ? 'bg-red-500' : 'bg-green-500')(class="py-2 px-5 rounded-lg w-full font-bold text-center text-white uppercase")= message

            if !error
                a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 hover:text-black uppercase mt-10 block") Iniciar sesión